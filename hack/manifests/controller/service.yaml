apiVersion: v1
kind: Service
metadata:
  name: goldilocks-controller-metrics
  labels:
    app.kubernetes.io/name: goldilocks
    app.kubernetes.io/component: controller
spec:
  selector:
    app.kubernetes.io/name: goldilocks
    app.kubernetes.io/component: controller
  ports:
    - name: metrics
      port: 8081
      targetPort: metrics
